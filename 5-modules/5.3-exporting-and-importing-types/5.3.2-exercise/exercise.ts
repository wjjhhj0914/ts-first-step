// --------------------------------------------------------------------------
// ✅ 타입 내보내기(Exporting)와 가져오기(Importing)
// --------------------------------------------------------------------------

// 1. person.types.ts 파일을 생성하세요.  
//    다음과 같은 프로퍼티를 가진 `Person` 타입 별칭(type alias)을 추가하세요.
//    - name: string
//    - age: number
//    - email: string


// 2. person.types.ts에서 `Person` 타입을 확장(extends)하는 
//    `Employee` 인터페이스를 정의하고, 다음 프로퍼티를 추가하세요.
//    - employeeId: number
//    - department: string


// 3. `Person` 타입과 `Employee` 인터페이스를 둘 다 export 하세요.


// 4. 이 파일 상단에서 `Person` 타입과 `Employee` 인터페이스를 import 하세요.


// 5. `person`이라는 이름의 변수를 `Person` 타입으로 선언하고,  
//    각 프로퍼티에 필요한 값을 할당하세요.


// 6. `employee`라는 이름의 변수를 `Employee` 타입으로 선언하고,  
//    각 프로퍼티에 적절한 값을 할당하세요.